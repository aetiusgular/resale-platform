# B4 — Browse + search (M4)

READ FIRST (whitelist): CLAUDE.md, docs/HANDOFF.md, docs/PROJECT_PLAN.md (M4),
docs/FEATURE_SET.md §1, docs/DESIGN_MAP.md row for Browse Results, and
design-reference/Browse Results.dc.html.

RUN CONTEXT (headless):
- .env.local exists — never print/cat/commit. Supabase CLI: export
  SUPABASE_ACCESS_TOKEN + SUPABASE_DB_PASSWORD parsed inline (never echo).
  Migrations: pnpm exec supabase db push --yes (ignore Docker warnings).
  Commit locally, no GitHub push.
- ANTI-HANG RULES (mandatory, as before): no watch modes / foreground
  servers; playwright owns app lifecycle; vitest run only; bound all
  test/build runtimes; hang twice = HANDOFF blocker + B4 BLOCKED + stop.

TASKS:
1. Search migration: tsvector generated column on listings
   (title+brand+description, weighted A/B/C), GIN index; price_history table
   (listing_id, old/new price_cents, changed_at) populated by trigger on
   price update; saves table (user_id, listing_id, created_at, unique pair)
   + saves_count denormalized on listings via trigger. RLS: saves owner-only
   rows, price_history public read for active listings. db-guard reviews.
2. /browse per the Browse Results design export:
   - Desktop: 240px left filter rail — MY SIZES toggle module (reads profile
     sizes jsonb; helper text + edit link), collapsible sections DEPARTMENT /
     CATEGORY / SIZE / DESIGNER / PRICE / CONDITION / SELLER LOCATION /
     SHOW ONLY with live counts (grouped count queries, cached 60s),
     CONDITION as compact 1–10 range with "7+ ONLY" toggle, SHOW ONLY with
     VERIFIED and PRICE DROPPED checkboxes. Monochrome checkboxes/chips —
     no blue anywhere.
   - Results header: "N LISTINGS FOR" + active-filter chips with ✕ +
     CLEAR ALL; FOLLOW SEARCH button (stores saved_searches row: user_id,
     query jsonb, created_at — notifications are PA, just persist);
     sort dropdown: relevance / newest / price ↑ / price ↓ / most saved.
   - Grid: 4-col desktop / 2-col mobile, listing cards per design system
     (timestamp, title, price, size + condition, VERIFIED tag, price-drop
     strikethrough from price_history).
   - Mobile: sticky "FILTERS (n)" button → full-screen drawer per design,
     "SHOW N LISTINGS" pinned button; chips scroll horizontally.
   - Empty state: EB Garamond italic "Nothing in the archive matches." +
     follow-search link.
   - URL-driven state: all filters/sort/query in searchParams (shareable,
     SSR-rendered first page, cursor-based LOAD MORE).
3. Full-text search wired to the header search input (B0 shell); relevance
   sort uses ts_rank. Search page IS /browse with ?q=.
4. Favorites: save/unsave on listing page + browse cards per design system
   (SAVED text toggle, no hearts on card images); saves count on listing meta.
5. SEO: SSR verified — listing + browse titles in server HTML (curl spec,
   no JS); meta/OG tags on listing pages (title, price, first image).
6. PostHog: lib/analytics.ts wrapper, env-flagged (off when key absent);
   events: $pageview, product_clicked, filter_applied, search_performed,
   listing_saved. No PII in event payloads.
7. Seed script extension: 30 varied fixture listings (distinct generated
   placeholder images — solid-color + text via sharp; NO real brand photos)
   across categories/sizes/conditions so browse is testable; idempotent.

VERIFY:
- e2e: filter size+condition returns correct seeded fixtures · q= search
  finds by title term · sort by price asc verified against fixtures ·
  save toggle persists across reload · follow-search persists row ·
  price-drop badge appears after seeded price change · mobile drawer flow.
- SSR spec: curl (JS disabled) contains listing title on /browse and
  /listings/[id].
- @live RLS: saves visible only to owner; saved_searches owner-only.
- ui-verifier: /browse vs Browse Results.dc.html at 1440 + 375px — fix
  reported deviations (rail structure, chips, typography, empty state).
- pnpm verify + verify:ui green incl @live locally.
- code-reviewer on diff (RLS + triggers — mandatory).

FINISH: ROADMAP (check B4) + HANDOFF (next=B5); conventional commit;
10-line summary ending "B4 COMPLETE — GATE GREEN" or "B4 BLOCKED: <reason>".
