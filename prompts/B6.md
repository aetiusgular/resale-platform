# B6 — Chat + offers (M6)

READ FIRST (whitelist): CLAUDE.md, docs/HANDOFF.md (includes B5 payment-flow
notes for offer-checkout reuse), docs/PROJECT_PLAN.md (M6), docs/DESIGN_MAP.md
row for Messages, and design-reference/Messages.dc.html.

RUN CONTEXT (headless):
- .env.local exists — never print/cat/commit. Supabase CLI: export
  SUPABASE_ACCESS_TOKEN + SUPABASE_DB_PASSWORD parsed inline (never echo);
  pnpm exec supabase db push --yes. Stripe CLI at ~/bin/stripe with
  --api-key from env when needed. Commit locally, no GitHub push.
- ANTI-HANG RULES as always. Realtime specs: subscribe with explicit
  timeouts; never wait unbounded for a message event.

TASKS:
1. Migrations (db-guard review): conversations (id, listing_id, buyer_id,
   seller_id, unique triple, comments_consent_buyer/seller booleans default
   false, timestamps), messages (id, conversation_id, sender_id, body,
   redacted boolean, created_at), offers (id, conversation_id, listing_id,
   from_user, amount_cents CHECK > 0, state enum open/countered/accepted/
   declined/expired/voided, expires_at, created_at). RLS: participants only
   for all three; messages INSERT via RPC only (for filtering); no client
   UPDATE on offers (state changes via server routes).
2. Link/redirect blocking on message insert (server-side RPC): reuse
   lib/antislop-config blocked patterns + URL regex + payment-app handles
   (paypal.me, cashapp, venmo, zelle, telegram/whatsapp). Violating content
   stored with redacted=true and body replaced by '[link removed — off-
   platform payment offers violate policy]'; system-style rendering per
   design.
3. Offer engine in lib/offers.ts: open → (accept | decline | counter |
   expire after 24h); counter creates new offer + closes previous
   (countered); accepted → 24h payment window: reuse B5 checkout with
   amount = offer amount_cents (server-verified against accepted offer row —
   extend orderAmounts to take an offer); unpaid after 24h → voided +
   strike row in buyer_strikes (user_id, reason, order/offer ref,
   created_at). Expiry/void via pg_cron hourly (same pattern as B5
   auto-release).
4. buyer_stats view (or materialized): purchases count, dispute count,
   strike count, avg time-to-pay bucket ("PAYS FAST" if median < 2h) —
   powers counterparty record lines. Used on: thread header (per design),
   B5 seller order view (replace stub if present).
5. Messages UI per Messages.dc.html: two-pane desktop (conversation list:
   username + tier badge stub + preview + timestamp + unread dot in accent),
   single-thread mobile with global tab bar; pinned listing context bar
   (thumbnail, Space Mono title, price, status chip); monochrome bordered
   bubbles (buyer left / seller right — NO colored bubbles); OFFER EVENT
   cards inline: amount Space Mono 700, ACCEPT primary / DECLINE secondary /
   COUNTER text button, countdown line "expires in Nh"; accepted-offer card:
   "PAY $X NOW" primary + saved-method line; declined offers collapsed with
   strikethrough. Consent banner (dismissible): transcript reference in
   disputes only if BOTH parties consent — toggle writes consent booleans.
   Supabase Realtime subscription for live message delivery + optimistic
   send.
6. MESSAGE SELLER button on listing page (B2 stub) now opens/creates the
   conversation. MAKE OFFER opens offer composer in thread.

VERIFY:
- e2e (@live): full negotiation — buyer offers 1000 → seller counters
  1100 → buyer accepts → PAY NOW → checkout at 1100 exactly (assert
  order amounts) → paid_held. Declined path renders collapsed.
- Expiry: backdate accepted offer 25h, run cron fn → voided + strike row;
  buyer_stats reflects it.
- Redaction: message with paypal.me link stored redacted, renders system
  line; clean message untouched.
- Realtime: second browser context receives message without reload
  (bounded wait ≤10s).
- RLS (@live): non-participant cannot read conversation/messages/offers;
  client direct UPDATE on offers rejected.
- Tamper: PAY NOW with mismatched amount → server rejects.
- ui-verifier: messages screen vs Messages.dc.html (1440 + 375).
- pnpm verify + verify:ui green. code-reviewer (payments touched) +
  db-guard passes mandatory.

FINISH: ROADMAP (check B6) + HANDOFF (next=B7); conventional commit;
10-line summary ending "B6 COMPLETE — GATE GREEN" or "B6 BLOCKED: <reason>".
