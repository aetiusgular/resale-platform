# B5 — Checkout + escrow + seller protection (M5 + M5b) — THE MONEY MILESTONE

READ FIRST (whitelist): CLAUDE.md, docs/HANDOFF.md, docs/PROJECT_PLAN.md
(M5 + M5b), docs/DESIGN_MAP.md row for Checkout & Orders, and
design-reference/Checkout & Orders.dc.html.

This is the highest-risk prompt of the build. Slow is fine; wrong is not.
Plan carefully before writing anything. EVERYTHING here gets code-reviewer
AND db-guard passes before commit — no exceptions.

RUN CONTEXT (headless):
- .env.local exists and now includes STRIPE_WEBHOOK_SECRET (from stripe
  listen for this sandbox). Never print/cat/commit env values.
- Stripe CLI is at ~/bin/stripe. It is NOT browser-logged-in — pass
  --api-key with the value of STRIPE_SECRET_KEY (parsed inline from
  .env.local, never echoed) on every stripe CLI invocation.
- For webhook e2e: run `~/bin/stripe listen --api-key <sk> --forward-to
  localhost:3000/api/webhooks/stripe` as a BACKGROUND process with bounded
  lifetime (start before the spec, kill by PID in a finally/teardown — never
  leave it running); prefer `~/bin/stripe trigger <event> --api-key <sk>`
  for deterministic event injection where possible.
- Supabase CLI: export SUPABASE_ACCESS_TOKEN + SUPABASE_DB_PASSWORD parsed
  inline. Migrations: pnpm exec supabase db push --yes.
- ANTI-HANG RULES as always: no watch/foreground servers; playwright owns
  app lifecycle; bound all runtimes; hang twice = HANDOFF blocker + BLOCKED.
- Commit locally, no GitHub push.

TASKS:
1. Stripe Connect Express onboarding for sellers: /settings payouts section
   → account link flow → payouts_enabled boolean on profiles updated ONLY
   via account.updated webhook. Gate: listings cannot go active (admin
   approve blocked with clear message) until seller payouts_enabled.
2. orders migration (db-guard review): id, listing_id unique-while-open,
   buyer_id, seller_id, amounts snapshot (item_cents, buyer_fee_cents,
   seller_fee_cents, shipping_cents, total_cents), stripe_payment_intent_id
   unique, state enum: paid_held → seller_confirmed → shipped → delivered →
   released | disputed | refunded | cancelled, tracking (carrier, number),
   timestamps per transition. order_events append-only audit table (order_id,
   from_state, to_state, source enum webhook/admin/cron/user, stripe_event_id
   unique nullable, payload jsonb, created_at). RLS: buyer/seller read own
   orders only; NO client writes — all mutations via server routes/webhooks
   (service role); admin read all.
3. State machine in lib/orders.ts: single transition function enforcing the
   legal edges above; every mutation inside a Postgres transaction (use a
   SECURITY DEFINER RPC transition_order() so atomicity lives in the DB);
   illegal transition = error + audit row with source + rejection.
4. Checkout: PaymentIntent on the platform account (separate charges &
   transfers pattern), amount from server-side re-computed fees (lib/fees.ts
   — NEVER trust client totals), metadata: listing_id/buyer_id/seller_id.
   Listing locked (status pending_escrow) on intent creation; released back
   to active on payment failure/timeout (webhook-driven). Checkout page per
   the Checkout & Orders design export: order summary card with fee line,
   address form (prefill from profile), card element, PAY button labeled
   "PAY $X — HELD IN ESCROW" + escrow explainer line.
5. Webhooks at /api/webhooks/stripe: signature verified with
   STRIPE_WEBHOOK_SECRET; idempotent via order_events.stripe_event_id
   unique constraint (insert-first pattern); handle payment_intent.succeeded
   (→ paid_held, listing → sold), payment_intent.payment_failed (unlock),
   account.updated (payouts_enabled), charge.refunded (→ refunded).
   Webhook route uses service role; NEVER getUser() there.
6. Fulfillment: seller confirm → shipped (carrier + tracking required);
   delivered via manual buyer confirm OR admin override (carrier-scan
   webhook is a stub TODO(PA) — document); buyer CONFIRM RECEIPT → release:
   stripe transfer to seller connect account of item_cents - seller_fee
   (+ shipping policy: buyer-paid passthrough), then state released.
7. Auto-release: pg_cron job (extension already enabled — fail loudly if
   not) every hour: orders in delivered where delivered_at < now() -
   interval '3 days' AND no dispute row → invoke release via the same RPC
   path (source cron). Config interval in one place.
8. Disputes: buyer opens within 72h of delivered (server-enforced), requires
   ≥1 photo upload, order → disputed (freezes auto-release); admin resolve
   → release or refund (stripe refund + state). disputes table w/ RLS
   (participants + admin).
9. Order-status pages per design: buyer view (timeline nodes with
   timestamps, auto-release countdown line, CONFIRM RECEIPT primary,
   REPORT AN ISSUE with 72h/photo requirements) and seller view (same
   timeline + buyer record row [purchases · disputes from a buyer_stats
   view] + payout line with countdown + PROTECTED card). Saved-payment
   methods: SetupIntent + saved card list + "use a different card" at
   checkout; PayPal = disabled text-only stub labeled "beta".
10. docs/STRIPE_TESTING.md: the listen/trigger workflow, test cards, how to
    time-travel the auto-release check in tests (injectable clock or
    delivered_at backdating in fixtures).

VERIFY (all @live specs run locally now):
- Happy path e2e (Stripe test mode): buy with test card → paid_held →
  seller confirm → ship w/ tracking → buyer confirm → transfer created —
  assert transfer amount in cents equals item - seller_fee exactly; listing
  sold; timeline renders all nodes.
- Refund path: admin refund from disputed → stripe refund → state refunded.
- Dispute freezes auto-release: backdate delivered_at 4 days, dispute row
  present, cron function runs, assert NO release.
- Auto-release fires: backdate 4 days, no dispute, cron function → released
  + transfer.
- Idempotency: replay same webhook event twice (stripe trigger or direct
  POST with same event id) → exactly one transition, one audit row.
- Client-total tampering: POST checkout with manipulated amount → server
  recomputes, rejects mismatch.
- RLS: third party cannot read an order; buyer cannot write state directly.
- Fee math unit tests extended: fee snapshot at order time survives later
  config change.
- ui-verifier: checkout + both order views vs Checkout & Orders.dc.html
  (1440 + 375).
- pnpm verify + verify:ui green. code-reviewer AND db-guard passes —
  address every HIGH before commit; log MED/LOW to HANDOFF.

FINISH: ROADMAP (check B5) + HANDOFF (next=B6, include payment-flow notes
for B6's accepted-offer checkout reuse); conventional commit; 10-line
summary ending "B5 COMPLETE — GATE GREEN" or "B5 BLOCKED: <reason>".
