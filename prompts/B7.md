# B7 — Community layer: legit-check threads + comments (M7)

READ FIRST (whitelist): CLAUDE.md, docs/HANDOFF.md, docs/PROJECT_PLAN.md
(M7 + decision D2), docs/USER_FEEDBACK.md §4 (the design changed BECAUSE of
community feedback — respect it), docs/DESIGN_MAP.md row for Listing Detail
(community section), design-reference/Listing Detail.dc.html.

RUN CONTEXT (headless):
- .env.local exists — never print/cat/commit. Supabase CLI: export
  SUPABASE_ACCESS_TOKEN + SUPABASE_DB_PASSWORD parsed inline (never echo);
  pnpm exec supabase db push --yes. Commit locally, no GitHub push.
- ANTI-HANG RULES as always.

TASKS:
1. Migrations (db-guard review): comments (id, listing_id, author_id,
   parent_id nullable for one-level replies, thread_type enum 'lc'|'general',
   body, status enum visible/removed/flagged, redacted boolean, pinned
   boolean, created_at), comment_actions (id, comment_id, actor_id, action
   enum 'agree'|'flag', unique (comment_id, actor_id, action), created_at).
   profiles: add verified_checker boolean default false + checker_category
   text nullable; listings: add comments_enabled boolean default true.
   RLS: read visible comments on active listings for everyone; INSERT via
   RPC only; no client UPDATE/DELETE (moderation via admin routes).
2. post_comment RPC (SECURITY DEFINER): requires profiles.id_verification_
   status = 'verified' (alpha note: also allow role='admin' for testing;
   the ID-provider integration is feature-flagged off, so seed script must
   mark founder accounts verified); rate limit new accounts (<30 days):
   2 comments/day enforced by count inside the RPC; thread_type 'lc'
   additionally requires verified_checker = true OR tier gold+ (tier column
   stub: add tier enum on profiles default 'bronze' if absent); reuse the
   B6 link/payment-redirect redaction pipeline on body.
3. Listing page community section per Listing Detail design export —
   replace the B2 placeholder: EB Garamond italic header "The community
   weighs in.", tabs LEGIT CHECK (n) / COMMENTS (n) (Inter 12px caps),
   pinned checker verdict card (1px accent border) support, comment rows:
   username Space Mono + tier badge + green VERIFIED CHECKER microtag where
   applicable + relative timestamp + AGREE (n) text action + flag action;
   input at bottom with helper "ID-verified members only · new accounts
   limited to 2 comments/day"; disabled state for unverified users; general
   tab hidden when seller toggles comments_enabled off (LC tab NEVER
   hideable); seller toggle in listing management UI; redacted comments
   render the system line per design.
4. Flag pipeline: flagged comment (>=2 flags or flagged by admin) → status
   flagged → appears in /admin/queue (extend existing queue with COMMENTS
   section); admin remove/restore/pin routes; comment_actions logged with
   actor pairs for future collusion analysis (analysis itself = PA, just
   keep the breadcrumbs queryable).
5. Verified-checker admin: admin route to grant/revoke verified_checker +
   category. Seed script: mark 2 founder fixtures as verified checkers,
   mark founder accounts id-verified, seed a demo LC thread on one fixture
   listing (pinned verdict + 2 comments).

VERIFY:
- e2e (@live): unverified account sees disabled input with helper text ·
  verified account posts in general · non-checker blocked from LC thread ·
  checker posts + admin pins verdict → renders as pinned card · 3rd comment
  same-day from new account rejected server-side · seller toggle hides
  general tab, LC persists · comment with cashapp handle stored redacted ·
  2 flags → comment lands in admin queue → admin removes → hidden.
- RLS (@live): client direct INSERT/UPDATE on comments rejected (RPC only);
  non-participant flag/agree dedupe (unique constraint).
- ui-verifier: listing community section vs Listing Detail.dc.html
  (1440 + 375) — pinned card, tabs, badges, input states.
- pnpm verify + verify:ui green. code-reviewer (RLS + rate limiting +
  redaction — mandatory) + db-guard passes.

FINISH: ROADMAP (check B7) + HANDOFF (next=B8); conventional commit;
10-line summary ending "B7 COMPLETE — GATE GREEN" or "B7 BLOCKED: <reason>".
