# B1 — Auth + invite gate (M1)

READ FIRST (whitelist — read nothing else beyond what these point to):
CLAUDE.md, docs/HANDOFF.md, docs/PROJECT_PLAN.md (M1 section),
docs/DESIGN_MAP.md rows for onboarding/invite screens, and the mapped
design-reference/Onboarding.dc.html.

RUN CONTEXT (headless — no interactive questions):
- .env.local exists with all keys. NEVER print, cat, or commit it.
- Supabase project is LINKED and the profiles migration is already applied
  remotely. For any supabase CLI command, export SUPABASE_ACCESS_TOKEN and
  SUPABASE_DB_PASSWORD by parsing .env.local inline (grep/cut into env vars,
  values never echoed). Push new migrations with: pnpm exec supabase db push --yes
- Docker is not available; ignore catalog-cache warnings from db push.
- Commit locally with conventional commits; do not push to GitHub.

TASKS:
1. Supabase email auth via @supabase/ssr: signup/login/logout, middleware
   using getUser() (NEVER getSession() for authz decisions).
2. invite_codes migration: code text PK (uppercase, format XXXX-XXXX),
   generated_by uuid → profiles, used_by uuid → profiles unique nullable,
   status enum('unused','claimed'), created_at, claimed_at. RLS: owner reads
   own generated codes; nobody reads others'. Redemption via SECURITY DEFINER
   RPC: atomic single-use claim (SELECT ... FOR UPDATE), case-insensitive
   input normalized, marks claimed + stamps profile invited_by. Explicit
   grants (auto-expose OFF). db push.
3. Middleware gate: no session → /enter; session but no claimed code →
   /enter. /enter per the Onboarding design: code entry (Space Mono caps
   input), designed error states ("code already used" in --alert), waitlist
   email capture for the codeless.
4. Signup flow per design: code → account (email/username/password) →
   ID-verification placeholder step (profiles.id_verification_status enum
   'unverified'|'pending'|'verified', default 'unverified'; UI card per
   design with VERIFY NOW disabled + "coming in beta" note; feature flag
   VERIFICATION_ENABLED=false in lib/flags.ts) → quick-setup stubs (sizes,
   address — skippable, store on profiles jsonb) → "your codes" screen
   showing 3 generated codes (Space Mono tokens, COPY buttons, status lines).
5. Code generation: 3 codes per newly activated member, format XXXX-XXXX
   from unambiguous alphabet (no 0/O/1/I). Seed script (scripts/seed-founders.ts):
   30 founder codes owned by a system profile; idempotent.
6. UI: build ONLY from the Onboarding design export + existing /styleguide
   tokens. Monochrome; accent green only for success/verified moments.

VERIFY (all must pass before finishing):
- Playwright specs: unauthenticated / redirects to /enter · bad code shows
  designed error · good code → signup → lands on codes screen with 3 codes ·
  reused code rejected with correct message · logout works.
- RLS/RPC unit spec (@live): second client cannot read another user's codes;
  concurrent double-claim of same code: exactly one winner.
- pnpm verify green; pnpm verify:ui green (non-@live); run @live specs too.
- Invoke the code-reviewer subagent on the full diff (auth touched — mandatory);
  address every critical finding.

FINISH: update docs/ROADMAP.md (check B1) + docs/HANDOFF.md (state, next=B2);
commit (conventional); print 10-line summary ending "B1 COMPLETE — GATE GREEN"
or "B1 BLOCKED: <reason>".
