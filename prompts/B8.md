# B8 — Analytics, hardening, alpha polish (M8) — FINAL PROMPT

READ FIRST (whitelist): CLAUDE.md, docs/HANDOFF.md (INCLUDING every deferred
finding from B5/B6/B7 — this prompt pays those debts), docs/PROJECT_PLAN.md
(M8), docs/FEATURE_SET.md §6, docs/USER_FEEDBACK.md §6 (founder seed list).

RUN CONTEXT (headless):
- .env.local exists — never print/cat/commit. Supabase CLI: export
  SUPABASE_ACCESS_TOKEN + SUPABASE_DB_PASSWORD parsed inline (never echo);
  pnpm exec supabase db push --yes. Commit locally, no GitHub push.
- ANTI-HANG RULES as always.

TASKS:
1. PAY THE DEBTS — work through every deferred item in HANDOFF explicitly:
   B6 MEDIUM double-lock on offer-based checkout (fix per the documented
   remedy); B5/B6 LOW items (shippingAddress → PI metadata, UUID validation
   on url params returning 400 not 500, offer-composer deep link); B1
   deferred (waitlist persistence: waitlist table + insert; email-verify
   enforcement decision: document as PA if out of scope, don't silently
   drop). Each fix gets a test. Anything consciously NOT fixed gets an
   explicit PA entry in docs/LAUNCH.md.
2. Rate limiting: middleware-level limits (pg-based counter or in-memory
   LRU per instance — document the tradeoff) on: auth endpoints (10/min/IP),
   uploads (30/hr/user), comments RPC (already limited — keep), checkout
   creation (10/hr/user), waitlist (5/hr/IP). 429 with Retry-After.
3. Error tracking: Sentry (@sentry/nextjs) wired but fully env-gated —
   no-ops when SENTRY_DSN empty; scrub PII (no emails/addresses in events).
4. Analytics: docs/ANALYTICS.md — PostHog dashboard definitions (funnel
   browse → listing → offer/buy; listing CTR; checkout abandonment;
   time-on-listing) with exact event/property names from lib/analytics.ts;
   add any missing events (checkout_started, checkout_completed,
   offer_made, offer_accepted, comment_posted). /admin/metrics page:
   listings by status, GMV (sum released orders), dispute rate, invite
   tree depth, signups by day — SQL views, admin-gated.
5. SECURITY SWEEP: run code-reviewer over the FULL diff B0→HEAD in focused
   passes (auth+invites / listings+storage+antislop / orders+payments+
   webhooks / chat+offers / comments+moderation). Write
   docs/SECURITY_REVIEW.md: every finding, severity, fixed-or-accepted
   with reasoning. ZERO unresolved HIGH/CRITICAL allowed. Fix everything
   fixable; MED accepted items need explicit justification.
6. Founder seeding: extend seed script — 24 founder accounts from
   docs/USER_FEEDBACK.md §6 usernames (fake emails founder+<name>@example
   .com, id_verified, 3 invite codes each; the 2 designated checkers per
   B7 seed stay). Idempotent, guarded to never run in production
   (NODE_ENV check + explicit --confirm flag).
7. docs/LAUNCH.md: production checklist — Vercel env vars (names only),
   Stripe LIVE cutover steps (marked HUMAN-ONLY: live keys entered in
   Vercel dashboard by the founder, never via CLI/agent; Connect platform
   profile on main account; live webhook endpoint + secret), Supabase
   prod hardening (backups on, point-in-time recovery note), domain +
   DNS, pg_cron verification, seed-script prod guard confirmation,
   PA backlog (carrier-scan webhook, PayPal via Commerce Platform,
   ID-verification provider, personalization feed, saved-search alerts,
   reverse-image detection, mobile PWA polish).
8. Performance: lighthouse against local prod build (pnpm build + start,
   bounded): /browse and /listings/[id] ≥ 85 performance score (relaxed
   from 90 for local variance); fix cheap wins only (image sizes, font
   display swap); log scores in HANDOFF.

VERIFY:
- Full suite: pnpm verify + pnpm verify:ui green including @live locally.
- Every debt-item fix has a passing test (double-lock: two accepted offers
  → second checkout cleanly rejected 409 with specific error).
- Rate-limit spec: 11th auth attempt in a minute → 429.
- Seed script: runs twice → no duplicates; refuses without --confirm.
- SECURITY_REVIEW.md exists with zero unresolved HIGH/CRITICAL.
- Lighthouse scores logged.

FINISH: ROADMAP (all checked) + HANDOFF (state: ALPHA BUILD COMPLETE;
remaining human steps list); conventional commit; 10-line summary ending
"ALPHA BUILD COMPLETE — ALL GATES GREEN" or "B8 BLOCKED: <reason>".
