# B2 — Listings + curation queue (M2)

READ FIRST (whitelist): CLAUDE.md, docs/HANDOFF.md, docs/PROJECT_PLAN.md (M2),
docs/FEATURE_SET.md §4, docs/DESIGN_MAP.md rows for Create Listing + Listing
Detail, and design-reference/Create Listing.dc.html + Listing Detail.dc.html.

RUN CONTEXT (headless):
- .env.local exists — never print/cat/commit. For supabase CLI: export
  SUPABASE_ACCESS_TOKEN + SUPABASE_DB_PASSWORD by parsing .env.local inline
  (never echo values). Push migrations: pnpm exec supabase db push --yes
  (ignore Docker catalog warnings). Commit locally, no GitHub push.
- ANTI-HANG RULES (mandatory): no watch modes, no foreground dev servers —
  playwright manages the app via webServer (reuseExistingServer false,
  120s timeout); vitest run only; wrap test/build bash calls so runtime is
  bounded (use `gtimeout` if available, else background + sleep + kill
  pattern); a command hanging twice = record blocker in HANDOFF, mark
  B2 BLOCKED, stop. Never wait indefinitely.

TASKS:
1. listings migration: id uuid PK, seller_id → profiles, title, brand,
   category, size, condition_score int 1–10 CHECK, condition_notes jsonb,
   price_cents int CHECK > 0, images text[] (6 slots), possession_photo_url
   text NOT NULL, status enum draft/pending_review/active/sold/removed,
   rejection_reason text, timestamps. RLS: anon/auth read active only;
   seller full CRUD own rows (except status transitions to active — admin
   only); admin role all. Explicit grants (auto-expose OFF). Indexes:
   (status, created_at desc), seller_id, brand. db-guard subagent reviews
   the migration before push.
2. Storage: product-images bucket (public read, authenticated write to own
   folder listings/{user_id}/); client-side resize to max 2000px JPEG before
   upload; 6 labeled slots per design: FRONT/BACK/TAG/DETAIL/FLAW/POSSESSION.
3. Sell flow at /sell per Create Listing design export: 4 steps
   (PHOTOS → DETAILS → CONDITION → PRICE), 1–10 rubric selector with
   definition lines, live fee math from lib/fees.ts single source
   (SELLER_FEE_BPS = 200, BUYER_FEE_BPS = 200; unit-test odd cents +
   min/max), submit → status pending_review, submitted state per design
   ("In the queue", position, SELLER PROTECTION card).
4. Admin queue at /admin/queue (profiles.role='admin' gate, middleware +
   RLS): pending list with all 6 photos, approve → active; reject → reason
   required, surfaces on seller's listing view. Functional-plain UI is fine
   for /admin (not a designed screen).
5. Listing detail at /listings/[id] per Listing Detail design export:
   gallery with labeled thumbnails, purchase panel (title/brand/size/
   condition + "what N means" popover/price block with fee line), TRUST
   STRIP placeholder rows, seller block (username, tier badge stub, VERIFIED
   ID microtag from id_verification_status), meta line. Community section:
   placeholder module marked "B7". BUY/OFFER buttons render disabled with
   "checkout arrives in B5" title. SSR — listing title must be in server
   HTML (SEO).

VERIFY:
- e2e: seller lists (6 photos incl possession) → pending → not publicly
  visible → admin approves → active → visible on /listings/[id] → reject
  path shows reason to seller.
- RLS specs (@live): non-owner cannot read pending/draft; non-admin cannot
  update status; storage write outside own folder rejected.
- fees unit tests; pnpm verify + verify:ui green incl @live locally.
- ui-verifier subagent: /sell and /listings/[id] vs the two design exports
  at 1440 + 375px; fix deviations it reports on tokens/fonts/missing elements.
- code-reviewer subagent on full diff (RLS + storage touched — mandatory).

FINISH: ROADMAP (check B2) + HANDOFF (next=B3); conventional commit;
10-line summary ending "B2 COMPLETE — GATE GREEN" or "B2 BLOCKED: <reason>".
