# B0 — Bootstrap: repo, context layer, design integration

GOAL: a deployable skeleton with the entire context layer, design tokens extracted
from the real design exports, and the verification harness working end to end.

RUN CONTEXT (headless — no interactive questions possible):
- .env.local ALREADY EXISTS in repo root with all keys. NEVER read it aloud,
  print it, cat it, or commit it. Reference variables by name only.
- docs/ already contains the seven planning docs. prompts/ contains this file.
- Supabase CLI auth may be MISSING (supabase login not yet run). If
  `pnpm exec supabase link` fails on auth: write the exact blocker + remediation
  command into docs/HANDOFF.md, keep the migration file ready in
  supabase/migrations/, and continue with everything else. Do NOT attempt
  browser logins.
- git remote is already configured (GitHub: aetiusgular/resale-platform).
  Commit locally; do not push.

TASKS:
1. Init: Next.js 15+ (App Router, TypeScript strict) + Tailwind + @supabase/ssr
   + supabase-js, pnpm only. Vercel-ready. Do NOT overwrite README.md, docs/,
   prompts/, or .gitignore (extend .gitignore as needed).
2. Design integration:
   a. Copy "/Users/tonyg/Desktop/Secondhand fashion design system" into
      design-reference/ (reference only — never imported by app code).
   b. Export format (verified): screen exports are HTML (*.dc.html — Browse
      Results, Checkout & Orders, Create Listing, Listing Detail, Messages,
      Onboarding, Seller Profile, Settings) plus tokens/, components/,
      ui_kits/, styles.css, guidelines/. Write docs/DESIGN_MAP.md: one row per
      .dc.html — file → screen → route → build prompt (B1–B8) that uses it.
      Prefer extracting tokens from tokens/ + styles.css over re-deriving.
   c. Tokens into code: tailwind config with --bg #FFFFFF, --ink #111111,
      --ink-soft #6B6B6B, --line #E5E5E5, --accent #1B4332, --alert #B3261E;
      next/font/google: Inter (UI), EB Garamond (serif), Space Mono (listing
      data); type scale 12/14/16/20/28/40; 2px radius; 8px grid. Build a
      /styleguide route rendering all tokens + a listing-card skeleton.
3. Context layer: write CLAUDE.md (<150 lines): stack versions; token/read
   discipline (read only whitelisted files; subagents explore); prices are
   integer cents; every table ships RLS in the same migration; getUser()
   never getSession() for authz; money-state changes in DB transactions,
   webhook-driven; VERIFY→RECORD→DECIDE session epilogue (per
   docs/BUILD_PROMPTS.md Part 1); model routing (plan on fable/opus, execute
   on sonnet).
4. Agents: .claude/agents/code-reviewer.md (model: opus; tools: Read, Grep,
   Glob only; reviews diffs for RLS coverage, authz, payment correctness;
   mandatory before committing auth/money/RLS changes), db-guard.md (sonnet;
   read-only; reviews migrations for RLS/indexes/cascades), ui-verifier.md
   (sonnet; playwright MCP + Read; screenshots routes at 1440/375px, compares
   against mapped design-reference/*.dc.html, reports deviation checklist).
5. Harness: pnpm verify = tsc --noEmit && eslint && vitest run; pnpm verify:ui
   = playwright test. Install playwright + chromium. One smoke spec:
   /styleguide renders and computed font-families include Inter, EB Garamond,
   Space Mono. Tag live-service specs @live (none yet). GitHub Actions:
   verify + non-@live verify:ui, dummy env values, no secrets.
   Seed docs/ROADMAP.md (B1–B8 unchecked, B0 checked when done) and
   docs/HANDOFF.md.
6. Env + permissions: create .env.example (variable names from
   docs/SETUP_CHECKLIST.md §3, blank values). Create .claude/settings.json
   allowlisting: pnpm *, git *, supabase *, stripe *, npx playwright *.
7. Supabase: pnpm add -D supabase (invoke via pnpm exec supabase — NOT
   installed globally). Attempt link to hosted project (project ref derived
   from NEXT_PUBLIC_SUPABASE_URL env var); on auth failure follow RUN CONTEXT.
   Write initial migration: profiles table (id uuid PK references auth.users,
   username unique, role text default 'member', id_verified boolean default
   false, created_at) + RLS (public read of username only via view or policy;
   owner update own row) + explicit grants (auto-expose is OFF on this
   project — grant select/insert/update to authenticated as appropriate).
   Push only if link succeeded.

ACCEPTANCE: pnpm build green · pnpm verify green · pnpm verify:ui green
(styleguide smoke passes) · DESIGN_MAP.md covers every .dc.html · CI workflow
file present · git log shows clean conventional commits.
FINISH: update docs/ROADMAP.md + docs/HANDOFF.md (state, blockers, next = B1),
commit everything (except env files), print a 10-line summary ending with
either "B0 COMPLETE — GATE GREEN" or "B0 BLOCKED: <reason>".
