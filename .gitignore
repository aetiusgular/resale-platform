# env — never commit
.env.local
.env*.local

# deps / build
node_modules/
.next/
dist/
out/

# test output
playwright-report/
test-results/

# misc
.DS_Store
*.log

# Supabase local dev (not used — we target hosted project only)
supabase/.temp/
audit-shots/
audit-findings.json
*.log

# quarantine for files removed in a cloud session (FUSE mount blocks rm)
_to_delete/

tsconfig.tsbuildinfo
.cowork_write_test
.playwright-mcp/
moderation-*.png
.claude/settings.local.json
.vercel
