# Every path is founder-owned. This routes review requests to @aetiusgular and,
# once branch protection / "require review from Code Owners" is enabled
# (GitHub Pro for private repos), makes that review mandatory.
# The agent-facing edit rules live in AGENTS.md; the CI-enforced subset is
# .github/protected-paths.txt.
* @aetiusgular
