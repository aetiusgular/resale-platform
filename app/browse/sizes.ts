/**
 * Re-export of the shared size scales (lib/sizes) for app-side importers.
 * The storage contract and validation live in lib/sizes so the API validates
 * exactly what the modal + settings render.
 */
export * from '@/lib/sizes'
