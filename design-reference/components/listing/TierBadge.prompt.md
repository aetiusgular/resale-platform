Seller tier as a text-only mono chip; rank reads as rising border/text contrast (Bronze soft → Gold ink border → Platinum --accent border), never metallic color or icons.

```jsx
<TierBadge tier="bronze" />
<TierBadge tier="silver" />
<TierBadge tier="gold" />
<TierBadge tier="platinum" />
```

- Space Mono 11px caps, 22px tall, 1px border, 2px radius (the global radius cap — no true pills).
- Platinum is the only tier touching --accent, border only.
- Sits inline beside usernames: `@objectdealer <TierBadge tier="gold" />`.
